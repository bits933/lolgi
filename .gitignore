# Dependencies (installed libraries — never uploaded)
node_modules

# Build output (auto-generated when you build the app)
dist
.vite
release/
out/

# Temporary / scratch files
tmp/
output/

# Personal machine settings (yours only — don't share)
.claude/settings.local.json
*.local

# Python cache
__pycache__/
*.pyc

# Markdown notes & docs — keep on your computer, don't upload
# (planning docs, debug reports, AI-tool config, etc.)
*.md

# AI-assistant tooling & planning docs — not app code, kept local only
.claude/
.codex/
.planning/
prd/

# Installer .exe — too large for GitHub (100MB limit); distribute via GitHub Releases instead
*.exe
*.blockmap

# Local checkpoint notes â€” keep on this machine, never push to GitHub
backups/
