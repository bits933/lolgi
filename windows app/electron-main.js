// Entry point for Electron — resolves the compiled main process.
// This file exists at the project root so Electron's module resolution
// finds the built-in 'electron' module before node_modules/electron.
require('./dist/main/main/main.js');
